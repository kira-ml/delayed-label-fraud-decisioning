# Daily Log â€” 2026-09-22

> **Repository:** `delayed-label-fraud-decisioning`
> **Course:** Introduction to Machine Learning â€” Final Group Project
> **Instructor:** Ken Oliver Caparros *(verify against the requirements PDF â€” it says Dr. Armida P. Salazar)*
> **Session focus:** Session 1 â€” pipeline verification, primary recalibration, Streamlit delivery. Session 2 â€” three pipeline fixes, EDA completion, Streamlit upgrades.

---

## âš¡ TONIGHT'S TODO (in order)

Only five things left. Everything else is done.

| # | Task | Where | Time | Blocked by |
|---|---|---|---|---|
| 1 | Write `documentation/data_dictionary.md` | derive from `notebooks/01_eda.ipynb` Â§1 + `docs/data_card.md` Â§4 | 30 min | nothing |
| 2 | Deploy Streamlit app to Streamlit Cloud â†’ get URL | share.streamlit.io | 30 min | needs `requirements.txt` and `models/*.pkl` + `*.json` committed or stubbed |
| 3 | Write `documentation/technical_documentation.md` | install, structure, run, limitations, troubleshooting | 45 min | nothing |
| 4 | Write IMRaD paper â†’ `paper/paper.docx` + `paper/paper.pdf` | IEEE format, 6â€“10 pages | 2â€“3 hrs | needs #1 for Methods section |
| 5 | Sign `documentation/contribution_record.md` + `documentation/ownership_declaration.md` | print, sign, scan to PDF | 20 min | needs all group members + instructor |

**Stop after #5.** Do not touch the pipeline. Do not add features to the app. The pipeline and app are frozen.

**If tired tonight:** do #1 and #3 (writing, low energy). Leave #4 for tomorrow.
**If fresh tonight:** #1 â†’ #4 â†’ #2 â†’ #5.

---

## âš ï¸ ONE OPEN QUESTION BEFORE YOU SIGN ANYTHING

The requirements PDF lists the instructor as **Dr. Armida P. Salazar**. Everywhere in your docs it says **Ken Oliver Caparros**. Figure out which is correct before you produce the ownership declaration â€” it needs to be signed by the right person, named correctly.

If you can't reach anyone tonight, leave the declaration for last.

---

## Session 2 â€” 2026-09-22 (today's fixes)

### 1. Category mapping fix (`train_baseline.py`, `score.py`, `calibration.py`)

**Problem.** `train_baseline.py` fit LightGBM with train-only category codes. `score.py` and `calibration.py` rebuilt them from train âˆª val. If val ever contained a value train didn't, integer codes would drift and predictions would silently change.

**Fix.** `train_baseline.py` writes `artifacts/categories.json` after training. `score.py` and `calibration.py` load it and use the exact mapping.

**Verification.** `Mean p_fraud: 0.012398` unchanged. Policy cost `0.007621` unchanged. Action distribution identical (206,276 / 19,473 / 1,742). Behavior-neutral.

### 2. Idempotent final-test append (`evaluate_compare.py`)

**Problem.** `evaluate_compare.py` appended `## Final Test Results` on every run. Re-running it would stack duplicate sections.

**Fix.** Before appending, cut the file at the marker if it exists, then write fresh.

**Verification.** Ran `evaluate_compare` twice. `findstr /C:"## Final Test Results"` returns exactly one line.

### 3. `--analyses` flag (`pipeline.py`)

**Problem.** `sensitivity.py` and `bootstrap.py` were standalone. Easy to forget on a rebuild.

**Fix.** Added `--analyses` flag. Runs supplementary pipeline, then sensitivity, then bootstrap.

**Verification.** `python -m src.pipeline --analyses` runs all three. Both reports rewritten.

### 4. Encoding fix (`train_compare.py`)

**Problem.** `train_compare.py` wrote `model_comparison.md` with Windows cp1252 encoding. The `Â±` character became byte `0xb1`, which broke UTF-8 read in `evaluate_compare.py`.

**Fix.** Two `write_text` calls now pass `encoding="utf-8"`. Existing report converted from cp1252 to UTF-8 in place.

### 5. EDA notebook completion (`notebooks/01_eda.ipynb`)

**Delivered.** 6 figures, each with a written Finding + Decision:

| Figure | File | Decision driven |
|---|---|---|
| 1 | `fig1_numeric_distributions.png` | Scaling policy per algorithm |
| 2 | `fig2_categorical_fraud_rate.png` | Per-algorithm categorical encoding |
| 3 | `fig3_numeric_by_target.png` | Retain all features |
| 4 | `fig4_correlation_heatmap.png` | Retain all; LR stability noted |
| 5 | `fig5_class_balance_and_drift.png` | Chronological split; Macro F1 |
| 6 | `fig6_outliers.png` | No outlier removal |

Exports `reports/eda_summary.json` with all numbers the paper will cite.

### 6. Streamlit app upgrades (`app/streamlit_app.py`)

Added:

- **Expected savings per row** + priority rank column
- **Three expected costs per row** (`approve` / `review` / `block`)
- **Prioritised worklist** (top 50 sorted by expected savings, CSV export of all rows)
- **Review capacity slider** (top-K by expected savings, coverage %)
- **Segment breakdown** (fraud rate + action mix by `payment_type` and other categoricals)
- **Drift banner** (warns when batch mean p_fraud is out of training distribution)
- **Cost what-if sliders** in sidebar (`false_positive_cost`, `review_cost`, `residual_fraud_loss`)
- **Derived thresholds panel** (shows `p_review`, `p_block` live)
- **3-algorithm comparison panel** in sidebar (reads `reports/cv_results.json`)
- **Enriched model card** (algorithm, training window, test window, features, metrics, ECE)

**Verification.** Screenshots confirm:
- Row 1 math checks out: `p = 0.8089`, `c_block = 0.0191` = min of `[1.5522, 0.2627, 0.0191]`, savings `1.5330` âœ“
- Batch savings `11.27 âˆ’ 3.75 = 7.52` â‰ˆ `7.53` (rounding) âœ“
- `66.8%` batch advantage âœ“
- Top-K = 10 captures `5.38 / 7.53 = 71.4%` âœ“
- Segment rows sum to 500 âœ“

---

## Session 1 â€” 2026-09-22 (earlier today)

### 1. Bootstrap fix â€” `src/common.py` path constants

`src/pipeline.py` failed with `ImportError: cannot import name 'DATA_PROCESSED'`. Extended `src/common.py` with path constants, `COSTS_PATH`, `load_costs()`. Removed duplicate `load_costs` from `decide.py`.

### 2. Dataset path alignment

Moved `Base.csv` to `data/original/Base.csv` (course-required layout). `load.py` searches both paths.

### 3. Supplementary pipeline first full run

Mean `p_fraud = 0.234` (19Ã— too high). Root cause: `is_unbalance=True` in `train_baseline.py` reweighted the positive class ~90Ã—. Removed. After fix: `p_fraud = 0.0124`, policy cost `0.0076`, 57.1% advantage over strongest baseline.

### 4. Primary pipeline first run

Selected LightGBM. Test Macro F1 `0.5186` with `is_unbalance=True`.

### 5. Documentation reconciliation

Filled `data_card.md` Â§5.4 row counts, updated `problem_framing.md` Â§12, replaced `YYYY-MM-DD` placeholders across 8 docs. (Encoding incident: PowerShell `Set-Content` wrote cp1252. Recovered via Python with `utf-8-sig` read + `utf-8` write.)

### 6. Test coverage â€” 60 tests

Added `test_data_schema.py`, `test_pipeline_integration.py`, `test_model.py`, `test_app_validation.py`. Fixed `choose_actions` signature to return 3 values. 60 passed.

### 7. Primary recalibration

Removed `is_unbalance=True` from `train_compare.py`. CV Macro F1 `0.5249 â†’ 0.5344`. Test Macro F1 `0.5186 â†’ 0.5336`. ROC-AUC unchanged (both models rank identically; difference is at the 0.5 boundary).

### 8. Streamlit app delivered

Batch upload + manual input, validation, analytics, cost-sensitive routing, model card sidebar. Dark slate theme. Verified against 4 input categories.

---

## Definition of Done â€” status

### Primary (Course Requirement)

- [x] Three traditional algorithms compared under identical conditions
- [x] 5-fold `TimeSeriesSplit` CV with mean Â± SD
- [x] Best model selected (LightGBM, CV Macro F1 0.5344)
- [x] Test set evaluated once on the untouched split
- [x] Macro F1, accuracy, precision, recall, ROC-AUC, confusion matrix reported
- [x] Streamlit app built, verified locally, and upgraded with operational features
- [x] **EDA notebook with â‰¥5 meaningful visualizations** â€” 6 figures, each with Finding + Decision
- [ ] **Data dictionary** â†’ `documentation/data_dictionary.md`
- [ ] **Streamlit Cloud deployment (URL)**
- [ ] **IMRaD paper (DOCX + PDF)**
- [ ] **Technical documentation** â†’ `documentation/technical_documentation.md`
- [ ] **Contribution record signed**
- [ ] **Ownership declaration signed**

### Supplementary (Project Depth) â€” complete

- [x] Delay simulator (1-month regime)
- [x] Chronological train / val / test split
- [x] Cost-sensitive policy over approve / review / block
- [x] Cost-based backtest with 5 baselines
- [x] Calibration measured (ECE = 0.0040, null stop)
- [x] Censored-label count reported (96,843 / 9.68%)
- [x] Sensitivity analysis on 4 cost parameters
- [x] Bootstrap CIs on policy advantage
- [x] `--analyses` flag runs both from the pipeline
- [x] Category mapping persisted across train / score / calibration
- [x] `evaluate_compare` idempotent on re-runs

---

## Numbers reference (for the paper)

| Quantity | Value |
|---|---:|
| Supplementary train / val / test rows | 397,039 / 278,627 / 227,491 |
| Supplementary censored rows | 96,843 (9.68%) |
| Primary train / test rows | 794,989 / 205,011 |
| Primary train / test fraud rate | 1.0253% / 1.4038% |
| CV Macro F1 â€” LR / RF / LGBM | 0.4783 / 0.4977 / 0.5337 |
| Selected model | LightGBM |
| Test Macro F1 | 0.5336 |
| Test ROC-AUC | 0.8766 |
| Test precision (fraud) | 0.3096 |
| Test recall (fraud) | 0.0424 |
| Test confusion matrix | TN 201,861 / FP 272 / FN 2,756 / TP 122 |
| Supplementary policy cost/txn | 0.007621 |
| Strongest baseline cost/txn | 0.017749 (LightGBM + static 0.5) |
| Policy advantage vs strongest baseline | 57.1% |
| Sensitivity range across all cost variations | 47.06% â€“ 64.54% |
| Bootstrap 95% CI on advantage | [53.16%, 61.24%] |
| Pytest result | 60 passed |
| ECE (validation) | 0.0040 (null stop) |

---

## Deferred â€” do not build for this submission

- SHAP per-row explanations
- Real-time streaming inference
- Automated retraining / drift detectors
- Multi-model ensembling
- Fairness metrics panel
- Threshold-tuning slider
- Reliability diagram in the app (no labels in uploaded batches)

Documented as future work in `docs/roadmap.md` and `docs/architecture.md` Â§10.


- Deployed URL: https://delayed-label-fraud-decisioning-gefp9s9mbkfdyzhhvescdm.streamlit.app
